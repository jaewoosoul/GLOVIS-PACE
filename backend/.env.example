PORT=4000
FRONTEND_ORIGIN=http://localhost:5173

# Claude 뉴스 분석 (backend/src/ai/news/*). API 키는 여기(백엔드)에서만 사용한다 — 프런트엔드로 전달 금지.
ANTHROPIC_API_KEY=
# 권장값: claude-haiku-4-5 (분류·추출 작업이라 빠르고 저렴한 티어로 충분). 비워두면 /api/news/analyze가 500을 반환한다.
ANTHROPIC_MODEL=
ANTHROPIC_TIMEOUT_MS=12000
